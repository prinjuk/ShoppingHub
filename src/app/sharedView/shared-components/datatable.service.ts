import { StoreList } from '../shared-components/datatable.model';
import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
@Injectable()
export class DatatableServiceStore {
  // private packetDataTable = new BehaviorSubject('default message');
  // currentMessage = this.packetDataTable.asObservable();



  // changeMessage(message: any) {
  //   this.packetDataTable.next(message);
  // }

}

